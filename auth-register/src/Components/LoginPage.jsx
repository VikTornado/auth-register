import React from 'react';
import {NavLink} from "react-router-dom";

const LoginPage = () => {
    return (
        <div>
            <NavLink to={"/register"}>
                fwefwefwef
            </NavLink>
        </div>
    );
};

export default LoginPage;